# Authoritative

Short **user-owned** notes that state **corrections, disambiguations, and adopted rules** for the vault when exports or secondary sources disagree or are vague.

- Assistants **read** these files and **cite** them in `wiki/` **`sources`** / **`## Sources`** alongside other `raw/` paths.
- Assistants **do not** edit, move, or delete files here unless you **explicitly** ask (same as the rest of `raw/`).

**Policy:** `AGENTS.md` → “**`raw/Authoritative/`** (authoritative corrections)”.
